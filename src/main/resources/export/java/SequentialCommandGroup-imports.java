#if ($command.getProperty("Requires").getValue() != "None")
//To Do Must develop a way to collect the required subsytem to import
//import ${package}.subsystems.#class($command.getProperty("Requires").getValue());
#end